[project]
name = "x12-parser"
version = "0.1.0"
description = "X12 parser for healthcare EDI 835 and 837 transactions"
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]

[project.optional-dependencies]
dev = ["pytest>=7"]
